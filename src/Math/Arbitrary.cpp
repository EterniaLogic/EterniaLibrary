#include "Arbitrary.h"

Arbitrary::Arbitrary(){
    bits=0;
}

Arbitrary::~Arbitrary(){
    data.clear();
}

// calculates # of bits used
int Arbitrary::bitcalc(){
    int v = 0;
    
    return v;
}
