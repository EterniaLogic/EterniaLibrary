#include "Planet.h"

// usesa sine wave to determine the density of the air according to the location.
double Atmosphere::getDensity(double relative){
     // D = (101325) / (287.05 * (15 + 273.15))
     // 
}
