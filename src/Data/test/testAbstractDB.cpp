#include "testAbstractDB.h"


void testAbstractDB() {
    /*AbstractDBTable table = AbstractDBTable(CharString("test.dat"), ADB_BASE_DYNAMIC, ADBT_NONE);

    // Create a few fields for the database
    AbstractDBField f1_key = AbstractDBField(DB_INT, true);
    AbstractDBField f2_name = AbstractDBField(DB_STRING);
    table.addField(f1_key);
    table.addField(f2_name);

    table.flushTable(); // flush data to the data file
    */
}
