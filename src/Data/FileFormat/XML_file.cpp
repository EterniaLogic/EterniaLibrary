#include "XML.h"


XMLRoot XMLRoot::loadFromFile(CharString filex){
    XMLRoot root;
    
    // special parsing required for XML
    
    return root;
}


void XMLRoot::toFile(CharString filex){
    // convert to XML file at filex
}
